やったこと
今日もprogateで基礎構文編を学習。
switch文、while文を復習。

詳細
とにかくswitch文ではcaseとdefaultにつく：、各処理とbreakには；を忘れずに。
case defaultと処理 breakに差をつけるインデント(字下げ)を意識するとわかりやすくなる。
while文はとにかく最後の変数の足し引きを忘れて無限ループが起こらないようにすることが大切。
例）numberが0より大きい→number > 0　そのまま日本語の順番にかくといいかも。
駄目じゃなさそうだけどそっちのほうが無難っぽい
