やったこと
スッキリわかるJava入門のChapter8でインスタンスとクラスを学習、終了。
オブジェクト指向に基づいたプログラムの作成手順を整理できてよかった。

詳細
１インスタンスの生成
→クラス名　変数名　＝　new　クラス名（）；
クラスからインスタンスを生成、変数に入れる。newは必須。
２フィールド値への代入
→変数名.フィールド名　＝　値；
フィールドに初期値をセットする。(名前の決定、最初の数値　とか)
３メソッドを呼び出す
→変数名.メソッド名（）；