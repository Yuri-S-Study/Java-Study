やったこと
結局スッキリわかるJava入門でChapter11を途中まで学習。
progateはお金を払わなくてはいけないのでそのうち。

詳細
抽象クラスの出し方
→public abstract 戻り値の型　メソッド名（引数リスト）;
詳細未定のメソッドということ。
抽象クラスを含むクラスは必ずabstractつきのクラスにしなければならない。
抽象クラスはnewによるインスタンス化が禁止されている。　
→ソースコードに抽象メソッドがなくとも親クラスから抽象メソッドを継承している場合でもabstractが必要。
→abstractをつけるか、未完成部分を完成させることで解決できる。
→未来の開発者にオーバーライドを強制できる。
多層階的に抽象継承構造をみてみると、階層が下がるたびにどんどん具体的になっていく。逆に上に行くたびに抽象的になっていく。
この抽象的な部分の中でもさらに抽象的な部分はインタフェースとして扱える。
→public interface インタフェース名  